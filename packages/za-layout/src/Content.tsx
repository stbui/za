import styled, { css } from 'styled-components';

const ContentStyle = styled.div`
    flex: auto;
`;

ContentStyle.defaultProps = {
    className: 'za-layout-content',
};

export default ContentStyle;
